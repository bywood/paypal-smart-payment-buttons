/* @flow */
/** @jsx h */

import { DEFAULT_VAULT } from '@paypal/sdk-constants/src';
import { h } from 'preact';

type MessageProps = {|
    ariaDescribeId: string,
    message: string,
    ref: HTMLElement
|}

export function Message() : mixed {
    return (
        <div
            className={}
            id={ariaDescribeId}
            
        ></div>
    )
}